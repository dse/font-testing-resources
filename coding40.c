#include <stdio.h>
#include <math.h>
#define R1(a,b,c)\
  (-1f*a+sqrt(1f*b*b-4f*a*c))/2f/a)
#define R2(a,b,c)\
  (-1f*a-sqrt(1f*b*b-4f*a*c))/2f/a)
int main(int argc, char** argv) {
    printf("Hello, %s!\n", "world");
    int a = 1, b = 2, c = 1;
    printf("%d\n",
}
